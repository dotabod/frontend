pre-commit:
  commands:
    fmt:
      run: |
        npx vp fmt {staged_files} \
          && git add {staged_files}
    lint:
      glob: '*.{js,jsx,ts,tsx,mjs,cjs}'
      run: |
        npx vp lint --fix {staged_files} \
          && git add {staged_files} \
          && npx vp lint {staged_files}
pre-push:
  commands:
    lint:
      glob: '*.{js,jsx,ts,tsx,mjs,cjs}'
      run: npx vp lint {push_files}
